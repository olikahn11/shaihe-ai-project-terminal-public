# 沙禾项目终端 4.4.4

这次修复账号/模型切换的一批体验问题，并加入不存凭据的账号记忆。

## 修复

- “切换模型终端”弹窗里未登录的终端之前显示为灰色、无法选择；现在未登录也可以选，切换后会提醒先登录，不再拦人。
- 统一“切换账号 / 切换模型 / 切换型号”的命名：切换账号＝同一家终端换登录账号；切换模型＝换到另一家 AI 终端（原来叫“切换模型终端”/“模型接力”，两个名字容易混淆）；切换型号＝同一家终端下换具体模型（原来叫“切换模型”）。帮助页新增专门说明这三者区别的段落。
- 修复账号切换完成后“取消换号”按钮一直残留、不会消失的问题：现在恢复到正常“等你输入”状态就会自动隐藏，不用手动点掉。
- 首页不勾选任何任务、直接点“切换账号/切换模型”时，新增确认提示，说明会切换哪家终端的登录账号、并顺带安全重启哪个任务，避免误操作到不想动的任务。
- **修复“取消”实际会让账号变成未登录状态的问题**：原来的换号流程是先退出旧账号、再登录新账号，如果中途取消或新登录失败，旧账号已经被清掉、回不来了。现在改为不提前退出——官方登录命令本身在已登录状态下重新运行，只有新账号真正登录成功才会覆盖旧凭据，取消或失败时旧账号从未被动过，天然就是“退回之前的登录”。

## 新增：账号记忆（不存凭据）

- 账号中心和切换账号弹窗新增“曾经登录过：xxx@xxx”提示，只记账号邮箱字符串，不保存密码、Token 或 Cookie，接口本身也不接受凭据参数。
- 每次切换仍然需要走一遍真实的官方登录页，这个记忆只是帮你想起用的是哪个邮箱，不是“免登录”。

验证：167 项 Python 测试（新增 12 项，覆盖“取消/失败绝不触碰旧账号”“账号记忆去重不存凭据”“换号完成后提示自动消失”等行为）、TypeScript 类型检查、Vite 构建、DMG CRC 校验、只读挂载、应用深度签名与实际启动均通过。

## 安装与兼容

- 本 Release 提供 macOS 13+ 完整工作端 DMG。
- 当前 macOS 包采用 ad-hoc 签名，尚未进行 Apple Developer ID 公证。
- 4.4.1 Windows、Android、iPhone 遥控端可继续连接 4.4.4 Mac 工作端，无需重新下载。
