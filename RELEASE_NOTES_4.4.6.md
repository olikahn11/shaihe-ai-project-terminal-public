# 沙禾项目终端 4.4.6

这是一次终端状态与 Codex 回看体验修复。

## 修复

- 首页不再只因为终端底部保留着输入框，就把正在 `Working` 或可中断的 Codex/Claude 任务误报为“等你输入”。
- 启动器会根据真实进程状态清理陈旧“已暂停”标记；暂停任务在终端收到第一次输入时，会先自动继续再发送输入。
- Codex 新建和继续会话使用官方 `--no-alt-screen` 行内模式，让 xterm 保留对话回滚。
- 升级前已在运行的旧 Codex 全屏会话无需重启：触控板上滑、“上翻/下翻/看最新”、`PageUp/PageDown` 和 `Shift+方向键` 可直接操作 tmux 保留的历史，不再把滚动转成 Codex 旧输入。
- 从左侧项目栏继续历史对话（或新建任务）后，首页任务栏不出现该任务：原生终端窗口打开后主窗口通常被遮挡、轮询暂停，而打开窗口这一步没有重绘主页，任务栏一直停在旧内容上。

## 新增

- 设置页底部显示当前实际运行的版本号，来源是运行中的程序本身而不是写死的常量，便于反馈问题时确认版本。

## 交互与性能

- Codex 和 Claude 继续使用各自官方终端，不强行改造成同一套假聊天界面；工具条和 Provider 静态色板尽量统一操作。
- 没有增加逐字输出解析、无限历史缓存或持续重绘；输出队列和 xterm scrollback 仍保持有界。

验证：Python `174/174`、TypeScript 类型检查、Vite 构建、真实浏览器点击“上翻/看最新”并确认 tmux `copy-mode` 进入/退出、DMG CRC/SHA-256、应用深度签名和健康接口全部通过。

## 安装与兼容

- 本 Release 提供 macOS 13+ 完整工作端 DMG。
- 当前 macOS 包采用 ad-hoc 签名，尚未进行 Apple Developer ID 公证。
- 4.4.1 Windows、Android、iPhone 遥控端可继续连接 4.4.6 Mac 工作端，无需重新下载。
