# 沙禾项目终端 4.4.9 发行说明

沙禾项目终端（Shaihe AI Project Terminal）是一款专为 Mac 开发者打造的高性能本地 AI 工作台，聚合多家官方 AI 命令行工具，提供统一会话管理、多账号隔离、低延迟输入和高颜值终端体验。

## 4.4.9 核心升级

### 1. 终端 0 延迟秒开 (Instant Mount)
- **消除后端事件循环阻塞**：移除了建立连接时冗余的同步进程调用，将 tmux 按键配置固化至全局配置文件中，WebSocket 握手耗时从数百毫秒降至微秒级；
- **前端独立终端骨架并发直连**：打开任务大窗口时，在第 0 毫秒立即挂载终端组件并并发建立连接，不再等待全量网络快照往返，输入提示符与光标秒开不卡顿。

### 2. 终端色彩与排版全面重构 (高饱和真彩体验)
- **深邃黑 OLED 背景**：告别泛灰绿色调，采用纯正 `#090a0f` 背景；
- **高对比度 ANSI 色彩阶梯**：标准 16 色与 Bright 16 色升级为高饱和度专业配色，翡翠绿、极光青、纯红、琥珀金各具特色；
- **解除对比度强行篡改**：关闭 xterm.js 默认降级改色机制，100% 忠实还原 TrueColor 与 256 色色块质感；
- **新增 ANSI 结构化排版组件**：支持双线边框、纯色背景高亮徽章与彩色分段进度条。

### 3. 官方接入 Google Antigravity (`agy` CLI)
- **居于核心第三席位**：主顶栏与后端注册表顺序保持一致（`Codex` → `Claude Code` → `Antigravity` → `Gemini` → `Grok`）；
- **全生命周期能力**：支持自动探测 `agy` CLI 二进制、新建会话、按会话 ID 恢复对话、多账号配置隔离（`ANTIGRAVITY_HOME`）及官方桌面端联动；
- **专属极光青主题**：标识符 `⟁`，配套包含 `/plan`、`/permissions`、`/compact`、`/status`、`/resume`、`/model`、`/review` 在内的安全快捷指令。

### 4. 智能前置 CLI 弹性安装引擎与账号交互重构
- **未安装 CLI 自动安装再登录**：点击登录时若系统未检测到官方 CLI，不再报错中断，而是先自动执行官方脚本或全局安装，完成后无缝调出官方登录授权；
- **全模型弹性级联安装与权限自动修复**：新增多策略降级体系（官方 bootstrapper 指定沙箱目录、npm 免 root 用户前缀 `--prefix ~/.local`、Homebrew 兜底）；智能规避 root 所有的 `~/.local` 或 `/usr/local` 权限拒绝（`EACCES`）；
- **终端交互式失败拯救外壳**：安装未完成时**绝不死屏断开（彻底告别 `Pane is dead`）**，终端保持交互，支持一键重试、沙箱免权限修复重试、手动指定/拖入已有 CLI 路径、或展示可复制官方命令并在后台静候按键直通；
- **账号隔离交互人类友好化**：去除了“沙禾独立账号”、“官方桌面端账号 · 不操作”等生硬技术名词，升级为“常用主账号 / 备用账号”与清晰安全隔离说明，动态智能切换“登录账号 / 登录另一个账号”；
- **专注真实终端模型**：清理了借用后端的纯 API 伪模型，确保列表中每一家大模型均具备完整官方交互终端与自主工具能力。

### 5. 手机端全功能与全能签 / TrollStore IPA 支持
- **配套 iOS 原生遥控 IPA**：发布包内新增 `Shaihe-Terminal-Remote-4.4.9.ipa`，专供 iPhone 用户通过全能签（Esign）、TrollStore、Sideloadly、AltStore 自签安装，内置扫码配对与网络重连；
- **免安装 Safari 扫码即用**：支持直接用 iPhone 扫码打开并“添加到主屏幕”作为全屏 Web App，随时随地随身掌控长线任务。

### 4. 多国语言支持 (Display Language)
- **支持 5 种主流语言**：简体中文 (`zh-CN`)、繁體中文 (`zh-TW`)、English (`en`)、日本語 (`ja`)、한국어 (`ko`)；
- **即时生效与持久化**：在设置页中任意切换，整个界面与各级面板即时刷新，并持久化保存；
- **无缝本地化**：顶栏、项目侧栏、任务总控、实况终端详情、底部快捷键栏与设置全流程完整翻译，方便全球用户顺畅使用。

---

## 软件安全性与脱敏保证

- 本安装包及生成的 `.dmg` 文件**仅包含可执行程序及前端资源，不包含任何个人凭据、Token、密钥或历史对话**。
- 所有使用者的 AI 凭据及会话均保留在用户本机的受保护应用目录中。
