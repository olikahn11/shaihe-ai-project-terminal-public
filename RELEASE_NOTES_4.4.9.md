# 沙禾项目终端 4.4.9

> 4.4.9 为 macOS 工作端重大性能与体验升级。Windows、Android 和 iPhone 遥控端继续保持完全兼容。

## 核心更新

### 1. 终端调出 0 延迟秒开 (Instant Mount)
- **消除后端事件循环阻塞**：移除了每次建立终端连接时多余的同步进程绑定，将配置固化至全局环境，让 WebSocket 连接耗时降至微秒级；
- **前端独立终端骨架即时并发**：打开独立大窗口时，在第 0 毫秒立即挂载终端组件并并发建立连接，无需等待全量网络快照，输入提示符与光标秒开弹出。

### 2. 终端色彩与排版全面重构 (高饱和真彩体验)
- **深邃黑纯正 OLED 背景**：采用纯正 `#090a0f` 背景；
- **高对比度 ANSI 专业色彩**：全面升级标准 16 色与 Bright 16 色阶（翡翠绿 `#22c55e`、极光青 `#06b6d4`、高亮红 `#ef4444`、琥珀金 `#eab308`、科技蓝 `#3b82f6`、霓虹紫 `#a855f7`）；
- **解除对比度强制降级**：关闭 xterm.js 默认改色篡改，100% 还原真彩色（TrueColor）与 256 色色块质感；
- **ANSI 仪表排版支持**：支持双线边框、纯色高亮徽章与彩色分段进度条。

### 3. 官方接入 Google Antigravity (`agy` CLI)
- **核心排位第三位**：顶栏与注册表顺序一致（`Codex` → `Claude Code` → `Antigravity` → `Gemini` → `Grok`）；
- **完整会话管理**：支持自动探测 `agy` CLI 二进制、新建对话、按会话 ID 恢复历史对话、多账号配置隔离（`ANTIGRAVITY_HOME`）及官方桌面端联动；
- **专属极光青主题**：标志符 `⟁`，配套包含 `/plan`、`/permissions`、`/compact`、`/status`、`/resume`、`/model`、`/review` 在内的安全快捷指令。

---

## 账号隔离与隐私保护

- 设置与首页顶栏统一管理“默认账号”；单个任务可在右侧独立换号，不影响默认及其他任务；
- 独立账号位隔离（`CODEX_HOME`、`CLAUDE_CONFIG_DIR`、`ANTIGRAVITY_HOME` 等），防止跨任务身份串扰；
- **公开发行安装包零隐私残留**：安装镜像不包含任何个人凭据、Token、密钥或历史对话，所有使用者的数据完全保留在各自电脑的受保护存储中。
