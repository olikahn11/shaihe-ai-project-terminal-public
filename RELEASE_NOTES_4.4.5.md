# 沙禾项目终端 4.4.5

本版集中改进项目侧栏、账号切换、额度提示、终端操作与中文交流体验。

## 项目与会话侧栏

- 项目清单进度和真实终端运行状态分开显示，不再把“以前运行过”误显示为当前进行中。
- 点击项目整行可展开当前任务和历史会话；历史对话按原 Provider 与官方 Session ID 继续，不从头建立新会话。

## 账号与额度

- 首页右侧栏和终端大窗口的“切换账号”进入真实账号换号流程，不再误打开切换模型终端。
- 支持选择当前账号、已记忆邮箱或其他账号作为官方登录提示；只记邮箱，不保存密码、Token 或 Cookie，新登录成功前不清除原账号。
- Codex 官方额度继续显示；Claude 增加手动额度刷新和更稳健的 `/usage` 解析。读取失败时显示原因，不伪造百分比。
- 一键功能暂不可用时显示具体原因和恢复方式，不再只有无法点击的灰色按钮。

## 终端交互与中文交流

- 大终端新增全选、复制、粘贴和“看最新”；上滑后进入回看状态，新输出不会把视图强行拉回底部。
- 优化滚动、方向键焦点、有界输出队列、scrollback 和 ANSI/真彩色层次，减少长输出与多窗口下的跳屏和卡顿。
- 新任务可见地默认勾选“优先使用简体中文”，用户可以取消；已有会话可在“一键功能”中点击“中文交流”。
- 代码、命令、路径、API 字段、报错原文、专有名词和官方交互选择器保留必要原文，避免翻译后无法执行。

## 验证与兼容

- Python 测试 `169/169`、TypeScript 类型检查、Vite 生产构建、DMG CRC/只读挂载、应用深度签名、本机安装和健康接口均通过。
- 本 Release 提供 macOS 13+ 完整工作端 DMG，采用 ad-hoc 签名，尚未进行 Apple Developer ID 公证。
- 4.4.1 Windows、Android、iPhone 遥控端可继续连接 4.4.5 Mac 工作端，无需重新下载。
