# 沙禾项目终端 4.4.3

这次是针对账号切换体验和额度显示的修复版本。

## 修复

- 切换账号时终端顶部新增醒目提示条：明确告诉用户“请去系统浏览器完成官方登录、这里暂时不用按键”，并提供“取消换号”按钮随时恢复原会话，不再让用户对着一段官方 CLI 原始输出猜下一步该做什么。
- 换号过程中，一键功能面板不再显示“模型仍在工作”这类容易误导的提示，改为准确说明“正在切换账号”。
- 修复账号中心刷新接口在遇到未知 Provider 时返回内部错误（500）的问题。

## 新增：手动“刷新额度”

- Claude Code、Grok CLI 目前没有不消耗对话的官方额度查询接口（不像 Codex 有专门的只读用量接口），4.4.2 为了避免后台隐藏消耗，直接关闭了这两家的额度显示。
- 4.4.3 加回了这项能力，但改成完全由用户主动触发：订阅额度面板新增“刷新额度”按钮，点一下才会发起一次真实的官方 `/usage` 请求，结果直接显示为额度条和重置时间；不点击不会消耗，任何后台轮询都不会自动触发这个请求。
- Codex 的额度依旧来自官方本地状态接口（`account/rateLimits/read`），本身就是真实数据、不产生额外消耗，未受影响。

验证：161 项 Python 测试（含新增的手动刷新用例与“绝不进入后台轮询路径”的架构测试）、TypeScript 类型检查、Vite 构建、DMG CRC 校验、只读挂载、应用深度签名与实际启动均通过。

## 安装与兼容

- 本 Release 提供 macOS 13+ 完整工作端 DMG。
- 当前 macOS 包采用 ad-hoc 签名，尚未进行 Apple Developer ID 公证。
- 4.4.1 Windows、Android、iPhone 遥控端可继续连接 4.4.3 Mac 工作端，无需重新下载。
