# 沙禾项目终端 4.4.2

本版把大模型终端里新手不容易记住的常用命令整理成中文按钮，并完成一次跨六家官方 CLI 的 Token 消耗整改。

## 新手一键功能

- 大终端底部右侧新增“快 一键功能”，根据 Codex、Claude Code、Gemini CLI、Grok CLI、Kimi Code、Qwen Code 显示对应操作。
- 新增“/ 命令助手”，点击只填入命令、不自动执行，确认后再按 Enter。
- 每个功能都有中文名称和独立 `!` 用途说明。
- 整合状态、模型、帮助、规划、压缩上下文、授权/权限、项目记忆初始化等常用能力。
- 任务暂停、终端未连接或输入框已有内容时，一键操作自动禁用，避免误发和覆盖草稿。

## Token 零额外浪费保护

- 六家终端空白启动和继续会话不再附加隐藏 system prompt、rules、developer instructions 或语言规则。
- 后台刷新不再调用 `-p /usage`、`-p /stats` 或任何模型提示通道。
- 后台只读取版本、登录配置、模型目录、本地会话记录和官方非生成状态接口。
- 账号中心常驻显示“Token 保护已开启”；高推理档位和并发任务会明确标为更高消耗。
- 修复前已经启动的暂停任务，首次继续时会使用原官方 Session ID 自动迁移到无隐藏提示的进程，不丢失对话历史。

验证使用六个隔离假 CLI 连续强制刷新 5 轮，命令日志中模型提示调用为 0；154 项 Python 测试、TypeScript 检查、Vite 构建、DMG 校验、只读挂载、签名和镜像内实际启动均通过。

## 安装与兼容

- 本 Release 提供 macOS 13+ 完整工作端 DMG。
- 当前 macOS 包采用 ad-hoc 签名，尚未进行 Apple Developer ID 公证。
- 4.4.1 Windows、Android、iPhone 遥控端可继续连接 4.4.2 Mac 工作端，无需重新下载。
